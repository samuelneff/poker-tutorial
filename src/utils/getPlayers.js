import fetch from 'node-fetch';

/**
 * Uses an API to get a list of player names.
 * @returns {Promise<Array.<Player>>}
 */
export default async function getPlayers() {

  // /////////////////////////////////////////
  //
  // TUTORIAL
  //
  // Use the `fetch` api to get a list of
  // users from an open API.
  //
  //   https://jsonplaceholder.typicode.com/users
  //
  // Lookup fetch on MDN
  //
  // Convert the response to an array of objects
  // and map the response to objects with a
  // playerName property.
  //
  // Function is declared as async, so use await
  //


}
